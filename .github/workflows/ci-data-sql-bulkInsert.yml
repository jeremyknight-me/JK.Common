name: 'Common.Data.Sql.BulkInsert CI'

on:
  workflow_dispatch:
  push:
    branches: [ "master" ]
    paths:
      - 'src/Common.Data.Sql.BulkInsert/**'
      - 'src/Common.Data.Sql.BulkInsert.Tests.Integration/**'
      - 'src/Common.Data.Sql.BulkInsert.Tests.Unit/**'
  pull_request:
    branches: [ "master" ]
    paths:
      - 'src/Common.Data.Sql.BulkInsert/**'
      - 'src/Common.Data.Sql.BulkInsert.Tests.Integration/**'
      - 'src/Common.Data.Sql.BulkInsert.Tests.Unit/**'

permissions:
  contents: read
  pull-requests: read

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: src

    steps:
    - uses: actions/checkout@v7
    - name: Setup .NET SDKs
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: 10.x
    - name: Build 
      run: dotnet build Common.Data.Sql.BulkInsert/JK.Common.Data.Sql.BulkInsert.csproj -c Release 
    - name: Format 
      run: dotnet format Common.Data.Sql.BulkInsert/JK.Common.Data.Sql.BulkInsert.csproj --severity error --verify-no-changes
    - name: Unit Tests
      run: dotnet test Common.Data.Sql.BulkInsert.Tests.Unit/JK.Common.Data.Sql.BulkInsert.Tests.Unit.csproj --verbosity normal
    - name: Integration Tests 
      run: dotnet test Common.Data.Sql.BulkInsert.Tests.Integration/JK.Common.Data.Sql.BulkInsert.Tests.Integration.csproj --verbosity normal
